// Action Types
const ADD_ORDER = 'FORM/ADD_ORDER';

// Action Creators


// Initial State
const initialState = {
  isAddingOrder: false
};

const reducer = (state = initialState, action) => {
  const { type, payload } = action;

  switch (type) {
    

    default:
      return state;
  }
}

export default reducer;